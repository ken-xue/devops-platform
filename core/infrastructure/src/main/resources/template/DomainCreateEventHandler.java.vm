package io.kenxue.cicd.application.application.${moduleName}.${allLowName}.handler;

import EventHandler;
import EventHandlerI;
import io.kenxue.cicd.coreclient.dto.common.response.Response;
import io.kenxue.cicd.coreclient.dto.${moduleName}.${allLowName}.event.${className}CreateEvent;
import lombok.extern.slf4j.Slf4j;
/**
 * ${comments}
 * @author ${author}
 * @date ${datetime}
 */
@Slf4j
@EventHandler
public class ${className}CreateEventHandler implements EventHandlerI<Response, ${className}CreateEvent> {

    public Response execute(${className}CreateEvent event) {
        log.debug("Handling Event:{}",event);
        return Response.success();
    }
}
